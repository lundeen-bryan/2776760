#ask for the users name
name = input("What is your name?")
#count the len of the name
name_len = len(name)
#print the number of characters in name
print("Hello " + name + "!" + "\nYour name has " + str(name_len) + " characters.")